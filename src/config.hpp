#pragma once
#include <SFML/Graphics.hpp>

namespace cfg
{

sf::Vector2u const window_size = {1000, 1000};
sf::Vector2i const grid_size = {3, 3};

float const cross_radius = 500;
float const circle_radius = 500;

}
